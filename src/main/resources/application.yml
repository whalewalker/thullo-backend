server:
  port: 8081

spring:
  profiles:
    active: ${PROFILE:dev}

  # DATASOURCE CONFIG
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA CONFIG
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect